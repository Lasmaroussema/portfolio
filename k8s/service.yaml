apiVersion: v1
kind: Service
metadata:
  name: JDSK-service
spec:
  selector:
    app: JDSK           
  type: NodePort         
  ports:
    - port: 80           
      targetPort: 4200   
      nodePort: 30080    
